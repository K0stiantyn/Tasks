<!-- // zad2/views/authors.ejs -->
<%- include('partials/navigation') %>
<% if (authors.length === 0) { %>
    <p>No authors has been found</p>
<% } else { %>
    <ul>
    <% authors.forEach(author => { %>
        <li><%= author.name %></li>
    <% }) %>
    </ul>
<% } %>